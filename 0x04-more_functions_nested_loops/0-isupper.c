#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase.
 * @c: character to check.
 * Return: 1 if c is uppercase, else return 0
 */
int _isupper(int c)
{
	f(c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
